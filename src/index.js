import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

// the beginning, rendering App component
ReactDOM
  .createRoot(document.getElementById('root'))
  .render(<App />)
